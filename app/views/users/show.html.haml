%p
  User: #{@user.first_name}
%p
  Email: #{@user.email if @user.email}

= form_for @user do |form|
  %p
    = form.text_field :like_name
    = form.submit "Add Like"
    
= form_for @user do |form|
  %p
    = form.text_area :like_box
    = form.submit "Add Likes"
    
%p Likes (#{@user.get_likes.num_likes}):
%script
- if @user.get_likes.num_likes == 0#find way of updating num_likes
  %p No Likes
- else
  - @user.get_likes.likes.each do |like|
    %p #{link_to StringFormatter.capitalize_words(like.name), like}