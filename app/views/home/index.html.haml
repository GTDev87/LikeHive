%h3 Home


- @users.each do |user|
  %p User: #{link_to user.name, user}
  
%p Search For Likes:
= form_tag likes_path, :method => 'get' do
  %p
    = text_field_tag :search, params[:search]
    = submit_tag "Search", :name => nil